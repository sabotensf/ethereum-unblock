{
  "name": "nexth",
  "version": "0.2.0",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "bun run --cwd packages/app dev",
    "prod": "bun run --cwd packages/app prod",
    "build": "bun run --filter '*' build",
    "test": "bun run --filter '*' test"
  },
  "trustedDependencies": [
    "@pokusew/pcsclite",
    "@reown/appkit",
    "unrs-resolver"
  ]
}
